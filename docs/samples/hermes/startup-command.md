# Hermes 启动命令

## 命令格式

```bash
hermes chat -q <消息内容> --yolo
```

## 参数说明

| 参数 | 说明 |
|------|------|
| `chat` | 聊天子命令 |
| `-q` | 安静模式（非交互） |
| `--yolo` | 自动批准工具调用 |

## 会话恢复

```bash
hermes chat -q <消息内容> --resume <session_id>
```

## 可执行文件路径

- 二进制名: `hermes`
