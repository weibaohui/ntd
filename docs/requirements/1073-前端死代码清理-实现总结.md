# 1073-前端死代码清理-实现总结

| 修改人 | 修改时间 | 修改内容 |
|--------|---------|---------|
| AI | 2026-08-17 | knip 扫描死代码清理（112 项 → 15 项）+ 评审修复 |

---

# 1. 背景与目标

- 用 `npx knip` 扫描前端死代码，逐项人工核对后清理，降低维护噪声。
- 纯清理 PR：不改任何运行时行为，仅删除无消费方的代码、把「文件内自用、外部无人 import」的 export 降级为模块私有。

# 2. 改动清单（首轮清理，commit 6085678d）

## 2.1 删除 3 个孤立测试文件

| 文件 | 说明 |
|------|------|
| `frontend/tests/cdp-ui-check-template.cjs` | CDP 调试模板，无 spec 引用 |
| `frontend/tests/check_dashboard_global_view.cjs` | 一次性调试脚本 |
| `frontend/tests/playwright.chrome.config.ts` | 独立 chrome 配置，未被任何 runner 使用 |

## 2.2 降级 11 个「文件内自用、外部无人 import」的 export

`CRON_PRESETS` / `LOOP_STATUS_META` / `renderLoopStatusTag` / `PHASE_RANKSEP` / `COMPLEXITY_COLOR` / `COMPLEXITY_LABEL` / `laneColorForStatus` / `MAX_PER_GROUP` / `formatChatTime` / `getExpertByName` / `getTodosPage`，均去掉 `export` 关键字。
另删真死代码：`useDefaultPageId` hook + 其无用 `useMemo` import。

## 2.3 降级 `executorInstallPrompts.ts` 中 13 个执行器的 26 个 `INSTALL_*` export

13 个执行器（AtomCode/ClaudeCode/CodeBuddy/CodeWhale/Codex/Hermes/Kilo/Kimi/Mimo/MobileCoder/OpenCode/Pi/Zhanlu）的 `INSTALL_*_ACTION_KEY` / `INSTALL_*_PROMPT` 仅被文件内 `EXECUTOR_INSTALL_PROMPTS` 表引用，降级为 `const`。

## 2.4 降级 56 个未用导出类型 + 删除 3 个死类型

覆盖 `types/stats.ts` / `types/loop.ts` / `types/process.ts` / `types/expert.ts` / `hooks/*` 等。
删除真死类型：`LoopDto` / `LoopStepExecutionDto` / `SkillTreeNode`（全仓无引用）。

# 3. 评审修复（双轴 review 后追加 commit）

| # | 问题 | 修复 |
|---|------|------|
| 1 | `types/loop.ts` 头注释仍写「LoopDto = 环路主表 DTO」、`GateResultDto` 注释仍提已删除的 `LoopStepExecutionDto`（注释与代码脱节，违 CLAUDE.md 注释规范） | 两处注释改写为现状；补充 LoopDto 删除原因（需求 044 环路瘦身） |
| 2 | `StatusTag`（tasks/constants.tsx）被 PR 描述误判为「knip 误报」，实际全仓无消费方（TodoCenterCard/TodoListView 各有局部实现），是真死代码 | 删除 `StatusTag` 及其唯一依赖者 `laneColorForStatus` |
| 3 | `ActionToolbar`（common/ActionToolbar.tsx）同样被误判「误报」，实际无任何 JSX 渲染方；仅其类型 `BatchActionItem` 被 useBatchActions 消费 | 删除整个组件文件；`BatchActionItem` 类型迁入唯一消费方 `useBatchActions.tsx`（模块私有，外部按结构化类型接入）；同步修正 TodoListView/useBatchActions 中提及 ActionToolbar 的过时注释 |
| 4 | `executorInstallPrompts.test.ts` 的 `expect(result?.prompt).toBe(CLAUDE_PROMPT)` 属 f(x)===f(x) 自证断言 | 改为内容相关性断言 `toContain('claude')`，并注明缘由 |
| 5 | `docs/testing/107-AI回归测试执行指南.md` §5.3 仍指引用已删除的 `cdp-ui-check-template.cjs`（悬空文档指针） | 改为指向 agent-browser 临场流程，注明模板脚本已删除 |
| 6 | 违 AI协作开发约定 §5.1：无实现总结文档 | 本文档补齐 |

# 4. 剩余 15 项 knip 报告（保留，均有核实依据）

- **12 个未用导出**（`CommandCard`/`VIRTUAL_NODE_RADIUS`/`ThunderboltOutlined`/`getExecutorColor`/`STATUS_COLOR`/`DetailSection`/`DetailField`/`LogViewHeader`/`RESUMABLE_EXECUTORS`/`loadDefaultExecutor`/`getDefaultExecutor`/`setDefaultExecutorCache`）：均为「文件内已用、外部未直接 import」——其中 `RESUMABLE_EXECUTORS`/`load*`/`get*`/`setDefault*` 经 `src/types/execution.tsx` barrel 再导出并被 App.tsx/TodoDrawer/useExecutorAdmin 真实消费；`VIRTUAL_NODE_RADIUS` 被 FlowEdge.tsx 导入；`CommandCard` 被使用并再导出。属 knip 对 barrel/间接消费的已知盲区。
- **3 个未用类型**（`FeishuHistoryMessagesPage`/`RecentCompletedTodo`/`TaskMention`）：被 `@/types` barrel 消费，tsc 验证无错，保留。
- 另有 11 处 `sqlite3` unlisted binaries（测试脚本 CLI 调用）与 1 处 `bundledApi` duplicate export，均非死代码。

# 5. 验证

| 指标 | 清理前 | 首轮清理后 | 评审修复后 |
|------|--------|-----------|-----------|
| 孤立文件 | 3 | 0 | 0 |
| 未用导出 | 50 | 14 | **12** |
| 未用导出类型 | 59 | 3 | **3** |
| **死代码总计** | **112** | **17** | **15** |

- ✅ `npx tsc --noEmit` 零错误（评审修复后复跑通过）
- ✅ `npx vitest run` 全量 54 文件 452 用例通过
- ✅ `npx knip` 复跑确认 15 项（12 导出 + 3 类型）
- ✅ 被删项全仓 grep 无残留引用；`playwright.config.ts` 的 testMatch/testDir 从未匹配被删的 3 个文件
