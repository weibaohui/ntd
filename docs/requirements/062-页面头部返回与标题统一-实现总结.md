# 062-页面头部返回与标题统一-实现总结

| 修改人 | 修改时间 | 修改内容 |
|--------|---------|---------|
| AI (pi) | 2026-08-06 | 初始版本 |
| AI (pi) | 2026-08-07 | 评审意见二轮处理（自评 2 条 + CodeRabbit 11 条复核） |

对应需求：`docs/requirements/062-页面头部返回与标题统一-需求.md`（用户确认 1b2a3c4a）
设计文档：`docs/design/062-页面头部返回与标题统一-设计.md`

## 实现了什么

### R1/R4/R5 返回按钮统一（PageCard 收口）

- `components/common/PageCard.tsx` 新增 `onBack?: () => void`、`backLabel?: string` props：
  传入 `onBack` 即在 extra 区**最右端**渲染统一样式返回按钮
  （`<Button size="small" type="text" icon={<ArrowLeftOutlined />}>`），文案默认「返回列表」。
  extra 与 onBack 均空时不渲染 extra 容器（保持无按钮页面布局不变）。

逐页改造（8 处手写返回按钮全部收口）：

| 页面 | 改动 |
|------|------|
| TodoDetailPage | 删 titleSuffix 手写按钮 → `onBack` |
| LoopDetailPage | 同上 |
| TaskDetailPage | 同上 |
| TasksPage 内嵌详情分支 | 同上 |
| TodoPostPage 帖子页 | 删 extra 手写按钮 → `onBack` + `backLabel="返回"` |
| WikiViewPage | 删手写原生 `<button>` → `onBack` + `backLabel="返回"` |
| WorkspaceLoopConfigPage | 删 icon 插槽返回按钮（icon 还原为 SettingOutlined）→ `onBack` |
| ProcessEditorToolbar（自建 Toolbar） | 返回按钮改 `small+text`、文案「返回列表」、保持操作区最右 |

移动端：`MobileHeader.showBackButton` 补上 `tasks` 视图（此前任务详情在移动端无返回入口）。

### R2 标题格式统一（`模块名 #{id}: {具体名称}`）

| 页面 | 改造后标题 |
|------|-----------|
| TodoDetailPage | `事项 #{id}: {title}`（不变） |
| TaskDetailPage | `任务 #{id}: {title}`（不变） |
| TasksPage 内嵌详情 | 固定「任务详情」→ `任务 #{id}: {title}`（接 onTitleReady） |
| LoopDetailPage | `环路 #{id}` → `环路 #{id}: {name}`（新增名称上提） |
| TodoPostPage | 裸标题 → `事项 #{id}: {title}` |
| WikiViewPage | `{slug}` → `Wiki: {slug}` |
| WorkspaceLoopConfigPage | `{ws} - 环路配置` → `环路配置: {ws}` |
| ProcessEditorToolbar | `{displayName} ({name})` → `工艺: {displayName}` + 保留 `(唯一名)` 次级文本 |

### 数据流改动（唯一一处）

`LoopStudioDetailPanel` 新增 `onTitleReady?: (name: string) => void` prop（仿 `TaskDetailPanel.onTitleReady`），
detail 加载后上报环路名称；LoopDetailPage 以函数式 setState 接收（同名 bailout，无 NTD-007 死循环风险）。
此前 `hideTitleRow=true` 时环路名称无处展示，本次顺带补齐。

### 注释同步

修正 6 处把操作按钮位置误写为 titleSuffix 的存量注释（实际早已是 extra，062 后返回按钮在 extra 最右端）。

## 范围外说明

`SkillMarketplace`「返回来源列表」与 `SkillFileBrowserModal` 移动端「返回列表」均为**面板/弹窗内部**的
视图切换导航，非页面级页头，不在本次统一范围。

## 测试与验证

- 新增 `components/common/PageCard.test.tsx`：6 个用例覆盖 onBack 渲染/默认文案/自定义文案/点击回调/最右锚点/空 extra 不渲染。
- 更新 `LoopDetailPage.test.tsx` / `TodoDetailPage.test.tsx` 的 PageCard mock（titleSuffix → onBack），并为环路名称上提新增 1 个用例。
- 新增 `tests/062-page-header-unify.spec.ts`：桌面端 7 页 + 移动端 2 页共 9 个 Playwright 用例。
- `npx tsc --noEmit` 零错误；`npx vitest run` 267/267 通过；Playwright 9/9 通过。
- 验证截图随 PR 评论发布（不入库）。

## 安全反思

纯前端 UI 重构：无 API/权限/数据存储变更；唯一数据流改动是环路名称经回调上提（已有数据的只读展示）。
所有标题值均经 React 文本插值渲染（自动转义），无 XSS 注入面；无新增高权限操作或不安全默认配置。

## 评审意见处理记录

### 一轮（自评，commit 78db17d4）
- LoopDetailPage 补 loopName 重置 effect；修正 2 处 history.back() 失准注释。

### 二轮（CodeRabbit 11 条复核）
修复 7 条：
1. 三个文档初始版本日期误写 2026-08-07（实际创建于 08-06 深夜）→ 更正并补修订行；
2. 需求文档 R2 行内代码尾随空格（MD038）→ 改写描述；
3. PageCard 渲染条件 `extra != null` 对 falsy extra（如 `cond && <X/>` 传 false）会渲染空容器
   → 改回真值判断 `extra || backButton`，与改造前 `extra && ...` 语义一致，补 2 个单测；
4. LoopDetailPage 重置 effect 依赖补 workspaceId（一轮只覆盖 loopId）；
5. todo-post 标题守卫从 `todo ?` 改为 `todo?.title ?`——title 为空串时回退「事项 #id」，不输出裸冒号；
6. TasksPage isDetail 分支补段落总览注释（标题回退策略/onBack 行为/布局说明）；
7. 062 spec 增加种子数据 beforeAll 探测：缺失时桌面端用例整体 skip（对齐 028 spec 先例）。

跳过 4 条（附理由）：
- 测试命名 `test_<fn>_<场景>`：该规则属后端 Rust 约定，前端 267 个既有测试均为 `it('...')` 描述式，保持一致；
- 「移动端 tasks 返回后 TasksPage 卡在详情态」：机理不成立——URL 带任务 id 时 App 渲染 TaskDetailPage，
  TasksPage 整体卸载无内部状态残留；已补点击回归用例（spec 第 10 条）实证；
- TasksPage 340 行应拆分：存量问题非本 PR 引入，宜另开重构需求；
- Playwright 用例自建/清理数据（Heavy lift）：以 skip 门控对齐仓库既有 spec 实践，完整 fixture 化留待后续。

## 已知限制

- ProcessEditor 因编辑器形态（未保存拦截、保存/删除控件）保留自建 Toolbar，仅对齐样式/位置/文案/标题，
  未强行套 PageCard；若未来编辑器重构可考虑完全统一。
- `titleSuffix` prop 保留（BlackboardPage 用于 MobileDebounceIndicator），但约定不再用于返回按钮。
