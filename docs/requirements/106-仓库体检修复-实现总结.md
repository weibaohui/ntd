# 106 仓库体检修复 — 实现总结

> 设计文档：`docs/design/106-仓库体检修复-设计.md`
> 来源：2026-08-15 全仓库体检（5 路并行审计 + clippy/test/tsc 静态检查）
> 范围：Critical 3 项 + High 全部 + Medium 全部，单一 PR。

## 修复清单

### Phase A — 安全域

| # | 位置 | 修复 |
|---|------|------|
| C1 | `main.rs:693` / `config.rs:17` | 绑定地址尊重 `cfg.host`（此前硬编码 0.0.0.0 忽略配置）；默认 host 维持 0.0.0.0（评审决定）；非回环绑定启动时 warn 提示无鉴权暴露面 |
| C2 | `handlers/session.rs` | 新增 `validate_session_id`（白名单 `[A-Za-z0-9_-]`），GET 详情 / DELETE 入口统一净化，堵住 `..%2F` 路径穿越读/删任意 `.jsonl`（3 个 scanner 入口全覆盖） |
| A3 | `handlers/bundled.rs` | skill content 接口复用 `skills.rs` 的 `resolve_skill_path_for_read` 双道防线 |
| A4 | `handlers/tasks.rs` | artifact locator 拒绝绝对路径/`..`/符号链接逃逸（canonicalize containment）；128KB 截断改按字符（消除多字节边界 panic） |
| A5 | `handlers/config.rs` | GET/PUT /config 响应抹掉 `sync_token`（不能 skip_serializing——同一 derive 还用于落盘） |
| A6 | `handlers/profiles.rs` | provider 详情 api_key 返回脱敏值（前4+****+后4）；update 识别掩码原样回传 = 不修改。与前端 maskKey 规则一致（二次打码幂等） |
| A7 | `handlers/sync.rs` | `server_url` 只允许 http/https；URL 变更即清空已存 token（换服务器必须重填，杜绝改 URL 外发旧 token） |
| A8 | `handlers/todo.rs` | `batch_delete_todos` 补 `verify_todos_belong_to_ws` |
| A9 | `handlers/tasks.rs` + `workspace_guard.rs` | 新增 `verify_task(s)_belongs_to_ws`；create（环路模式校验 loop 归属 + 去掉 `unwrap_or(1)`）、get/delete/batch-delete/create-execution 全部接上 |
| A10 | `handlers/workspace.rs` + `db/todo.rs` | 删 workspace：不存在 404、仍有 todo 409（新增 `count_all_todos_by_workspace`，含归档与载体） |

### Phase B — 飞书链路

| # | 位置 | 修复 |
|---|------|------|
| C3 | `feishu/channel.rs` | 重连计数改「连续失败」语义：连接健康存活 ≥60s 后断开即复位；正常关闭复位 |
| C3 | `services/feishu_listener.rs` | listen() 返回后 supervisor 带退避重启（30s→上限 600s），bot 不再永久死亡 |
| B2 | `feishu/sdk/ws_client.rs` | pbbp2 应用层 pong 刷新 `ping_time`，心跳看门狗不再自杀式断连 |
| B3 | `feishu/sdk/config.rs` + `feishu_api_client.rs` 等 | 新增 `build_client_with_timeout`（15s 默认）；`req_timeout` 字段真正接线；api_client 换共享单例 Client（OnceLock）；sync 状态拉取、agent_bot OAuth、history_fetcher 全部带超时 |
| B4 | `services/feishu_push.rs` | 日志预览 `&text[..60]` 改 `chars().take(60)`，消除推送循环 panic 点 |
| B5 | `feishu/sdk/ws_client.rs` + `cache.rs` | 事件级 LRU 去重（event_id/message_id，容量 4096 / TTL 10min），重连重投不再重复执行 |
| B6 | `feishu/channel.rs` | try_send 丢弃日志带 message_id 便于对账 |

### Phase C — loop_runner 流转

| # | 位置 | 修复 |
|---|------|------|
| C-1 | `services/loop_runner.rs` `resolve_next` | `goto` 按 `gates_passed` 成败取 `success_goto`/`fail_goto`（此前 or 链不分枝，失败跳成功目标） |
| C-2 | 同上 | 裸环节名策略（installer 写入的 `"write-prd"` 等）按跳转目标解析，与 `transition_resolver` 语义对齐（此前落 `_` 兜底错误推进 next） |

### Phase D — 后端并发/资源

| # | 位置 | 修复 |
|---|------|------|
| D1 | `message_debounce.rs` | DebounceEntry 加 generation：被 abort 的旧 timer 触发时发现 entry 已被新 push 重建，归还消息不执行（修静默丢批竞态） |
| D2 | `message_debounce.rs` | p2p drain 结束后显式 drop guard 清 running 标记并复查队列，窗口期入队的消息不再滞留 |
| D3 | `executor_session.rs` | group_spawn 进程组 + kill 整组；stdout/stderr 行缓冲 10k 行上限；stdout EOF 后先排空 stderr 再 wait；wait 包 300s 超时 |
| D4 | `blackboard_debouncer.rs` | timer 分片 sleep（5s）+ 每片重读最新阈值：调大防抖即时生效；状态被清除时提前退出 |
| D5 | `log_flusher.rs` | flush 失败回滚改为旧条目回 buffer 头部，日志时间序不再乱序 |
| D6 | `executor_service/log_capture.rs` | stdout/stderr 读取循环遇非法 UTF-8 行跳过继续（此前静默终止丢后续日志） |
| D7 | `services/worktree.rs` | 同 repo 路径进程级互斥锁（防并发 git init 踩 index.lock）；rm -rf 兜底后补 `git worktree prune` |
| D8 | `git_sync/mod.rs` | git 命令 120s 超时；SSH `StrictHostKeyChecking` no → accept-new |
| D9 | `auto_update.rs` | npm view 60s / install 600s 超时，不再拖死更新调度循环 |

### Phase E — 前端

| # | 位置 | 修复 |
|---|------|------|
| E1 | `Dashboard.tsx:113` | `getFeishuMessageStats(undefined, hours)`——参数错位导致统计恒空 |
| E2 | `LoopStudioDetailPanel.tsx` / `TaskDetailTabs.tsx` | 渲染期 `JSON.parse(abnormal_handler_trigger_on)` 加 try/catch |
| E3 | `MessagesPage.tsx` | 全部筛选变更回第 1 页（含 bot 切换）；loadMessages 加 reqSeq 竞态守卫 |
| E4 | `AssistantManagementPage.tsx` | 绑定弹窗建连窗口期取消：cancelled 标志 + await 返回后立即 close EventSource |
| E5 | `useProcessPersistence.ts` + `useLeaveGuard.ts` | 返回按钮改为导航**前**确认；「留下」分支补发 popstate 让视图与 URL 重新同步 |

## 验证

- `cargo clippy --all-targets -- -D warnings`：**零告警零错误**
- `cargo test`（全量，多轮）：**28 个套件全部通过**（lib 1751 + 集成，0 失败）
- `npx tsc --noEmit`：**零错误**
- `npx vitest run`：**53 个测试文件 429 个用例全绿**
- 二进制冒烟：`cargo build` + `ntd --version` 正常
- 修复过程中引入并修复的两个测试问题：
  - `test_config_default` 断言注释随绑定行为修复同步更新（默认值本身维持 0.0.0.0）
  - `executor_session` 双流测试偶发失败：根因是 stdout EOF 分支不排空 stderr 缓冲（真实缺陷，非测试问题），已修

## 新增测试

- `validate_session_id` 3 例（UUID 放行 / 穿越 / 边界）
- `verify_task_belongs_to_ws` 3 例（ok / 跨 ws / 不存在）
- `resolve_artifact_file_path` 3 例（正常 / 穿越 / 符号链接逃逸）
- `mask_api_key` 3 例（长 key / 短 key / 前端二次打码幂等）
- `resolve_next` 回归 2 例（失败分支取 fail_goto / 裸名策略跳转）

## 已知遗留（Low 级 15 项，未在本 PR）

见体检报告 Low 清单：webhook 触发端点可枚举、同步历史 limit 无上限、systemd-run 僵尸进程、plist XML 未转义、token 前缀日志、WS 分片 sum/seq 无校验、前端若干低危竞态等。完整 API token 鉴权体系为独立特性另行立项。
