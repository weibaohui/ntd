# 111-事项环路看板时间过滤-测试

| 修改人 | 修改时间 | 修改内容 |
|--------|---------|---------|
| AI | 2026-08-16 | 初始版本 |

> 对应需求：`docs/requirements/111-事项环路看板时间过滤-需求.md`；设计：`docs/design/111-事项环路看板时间过滤-设计.md`

---

## 1. 单元测试（新增/修改）

| 测试 | 位置 | 覆盖点 |
|------|------|--------|
| `test_todo_center_page_hours_filter_window` | `backend/src/db/todo.rs` | hours=24 只命中窗口内事项；total/bucket_counts 同口径收窄 |
| `test_todo_center_page_hours_zero_or_none_means_no_filter` | `backend/src/db/todo.rs` | hours=0 / None 与全量结果一致（不过滤语义） |
| 既有 `TodoCenterPageQuery` 构造点 | `backend/src/db/todo.rs` | 全部补 `hours: None`（编译驱动，行为不变） |
| `TodoListPage` 相关用例（更新） | `frontend/src/components/todo-list/TodoListPage.test.tsx` | 时间分段渲染；切换档位后请求带 hours；running 形态不渲染 |
| `LoopListHeader` 用例（新增） | `frontend/src/components/loop-list/LoopListHeader.test.tsx` | list/kanban 均渲染「全部」分段；onHoursChange 回传 null/数值 |
| `LoopListPage` 用例（更新） | `frontend/src/components/loop-list/index.test.tsx` | 列表形态按 created_at 窗口过滤；「全部」恢复全量 |

## 2. 质量门禁（执行结果，PR 中更新）

| 门禁 | 结果 |
|------|------|
| `cargo clippy --all-targets -- -D warnings` | ✅ 零告警零错误（CLIPPY_EXIT: 0） |
| `cargo test`（lib + 集成套件） | ✅ 全部通过（0 failed，含新增 2 个 hours 单测） |
| `cd frontend && npx tsc --noEmit`（含于 build） | ✅ 零错误 |
| `cd frontend && npm run build` | ✅ 构建成功（30.8s，无新增告警） |
| Vitest（5 个相关文件） | ✅ 38 passed / 0 failed |

## 3. 功能实测（Playwright，独立端口 dev 实例）

> 说明：本需求开发在独立 worktree 进行，验证使用独立端口/数据目录的 dev 实例，
> 不干扰他人正在运行的 18088 实例。截图证据发布到 PR 评论，不提交仓库。

| # | 用例 | 方法 | 预期结果 |
|---|------|------|---------|
| 1 | 事项列表时间过滤 | 打开 /#/todos?view=list，选 6h | 表格仅剩近 6h 创建的事项；分页总数=过滤后总数 |
| 2 | 事项列表「全部」 | 选「全部」 | 恢复全量；刷新后回到默认「全部」（不持久化） |
| 3 | 事项卡片时间过滤 | 切卡片形态（?view=card），选 24h | 五类 Tab 卡片与角标同口径收窄；切回「全部」恢复 |
| 4 | 环路列表时间过滤 | 打开 /#/loops?view=list，选 7d | 仅剩近 7 天创建的环路 |
| 5 | 环路看板「全部」 | 切看板形态（?view=kanban） | 默认选中 24h；点「全部」执行历史不再收窄 |
| 6 | 回归：任务页看板 | 打开 /#/tasks?view=kanban | 时间过滤行为与改动前一致 |

## 4. 未覆盖项与说明

- 移动端（窄屏）header：本需求明确不接入，不做 Playwright 覆盖。
- 环路看板「全部」+ 大量环路时的性能：每环路执行历史仍限 20 条，无新增请求放大面，不做压测。
- 归档事项 Tab 的时间过滤：统一口径生效，已写入需求 §10 作为已知行为。
