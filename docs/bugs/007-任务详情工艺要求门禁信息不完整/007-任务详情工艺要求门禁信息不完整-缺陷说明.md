# NTD-007 任务详情工艺要求门禁信息不完整 — 缺陷说明

| 修改人 | 修改时间 | 修改内容 |
|--------|---------|---------|
| AI(Kimi) | 2026-07-30 | 初始版本 |

---

## 1. Bug 基本信息（Identity）

- Bug ID：`NTD-007`
- 所属系统 / 服务：`frontend`（`frontend/src/components/tasks/TaskDetailPanel.tsx`）
- 首次发现时间：2026-07-30
- 发现来源：用户（`http://localhost:18088/#/tasks/16` 页面反馈）
- 当前状态：已确认存在

## 2. Bug 是否被确认存在（Existence）

- [x] Bug 已被稳定复现
- [ ] Bug 存在但不可稳定复现
- [ ] Bug 是否存在尚不确定

### 2.1 复现环境

- 环境类型：开发环境（端口 18088）
- 系统版本 / Commit ID：`feat/recipe-editor` 分支当前工作区
- 相关配置项：任务详情页「工艺要求」tab

## 3. 触发条件（Trigger Conditions）

### 3.1 前置条件

- 系统状态：任务详情页可打开，且任务关联的工艺步骤配置了门禁
- 数据状态：至少一个 `loop_steps.gate_config` 中存在 `ai_criteria_review` 类型门禁，且该门禁配置了 `min_score`

### 3.2 输入条件

- 请求类型 / 参数：用户访问 `/#/tasks/:id` 并切换到「工艺要求」tab
- 数据范围或特征：AI 评审门禁需要展示通过阈值

### 3.3 时序 / 并发条件

- 是否必须并发：否
- 是否依赖调用顺序：否

## 4. 实际行为（Observed Behavior）

- 实际行为：工艺要求 tab 中门禁 Tag 只显示门禁名称和类型中文标签，例如 `AI 评分达标 (AI 评审)`。
- 返回结果 / 错误码 / 异常信息：无接口错误；属于前端展示信息不完整。
- 发生频率：必现。

## 5. 期望行为（Expected Behavior）

- 系统应当执行：环节下存在门禁时，门禁 Tag 应展示完整判定信息；对 `ai_criteria_review` 门禁，必须明确显示 AI 评审通过阈值（例如 `阈值 ≥ 80 分`）。
- 明确允许的正常结果范围：其他类型门禁可展示关键配置（产物名 / 脚本 / 等待超时），但不改变后端接口语义和既有页面布局。

## 6. 行为偏差定义（Deviation）

> **在任务详情工艺要求 tab 查看带 AI 评审门禁的环节时，系统实际只显示门禁名称和类型，但期望同时显示该门禁的完整判定条件（至少包括 AI 评审通过阈值）**

## 7. 影响范围（Impact）

- 受影响模块 / 功能：任务详情页「工艺要求」tab
- 受影响用户或场景：需要判断某个环节门禁为何通过 / 不通过的用户；尤其是 AI 评审阈值不明确的场景
- 是否存在以下风险：无数据错误、无数据丢失、无安全风险、无服务不可用；属于可理解性缺陷
- 影响范围是否已知完全：是

## 8. 已知边界与非问题（Non-Issues）

- 在以下条件下，当前行为是正确的：环节未配置门禁时不显示门禁行。
- 以下相似现象不属于本 Bug：执行历史 tab 中门禁评价结果展示；本 Bug 只处理工艺要求 tab 的静态门禁配置展示。
- 不需要在本 Bug 中解决的问题：工艺模板编辑器、LoopStudio 门禁配置交互、后端门禁评价逻辑调整。

## 9. 事实证据（Facts & Evidence）

- 用户反馈页面：`http://localhost:18088/#/tasks/16`
- 用户反馈事实：当前仅显示 `门禁 AI 评分达标 (AI 评审)`，看不出通过阈值是多少分。
- 代码事实：`TaskDetailPanel.tsx` 的 `StepInfo.gate_config` 类型只声明 `{ name, type }`，渲染只使用 `g.name` 与 `gateLabel(g.type)`。

## 10. 不确定点与待澄清事项（Uncertainties）

- 无关键未决项。用户已给出明确示例：AI 评审需要写清通过阈值分数。

## 12. 进入下一阶段的判定（Gate）

- [x] Bug 描述完整，事实清晰，可进入分析阶段
