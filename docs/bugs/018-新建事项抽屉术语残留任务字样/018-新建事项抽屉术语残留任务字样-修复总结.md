# NTD-018 新建事项抽屉术语残留「任务」字样 — 修复总结

| 修复人 | 修复时间 | 分支 / PR |
|--------|---------|-----------|
| Claude | 2026-08-17 | `fix/018-todo-drawer-wording` |
| Claude | 2026-08-17 | 评审修复：补 6 处文案行注释；缺陷分析补变更记录表；回归用例拆分为 2 条并钉 `selected_workspace`、改相对 baseURL、截图入 `test-results/` |

## 修改清单

| 文件 | 改动 |
|------|------|
| `frontend/src/components/TodoDrawer.tsx` | 标题「创建任务/编辑任务」→「创建事项/编辑事项」；占位「任务标题」→「事项标题」；「描述完成该任务…」→「描述完成该事项…」；提示「请输入任务标题」「任务已更新」「任务创建成功」→ 对应「事项」版本 |
| `frontend/src/components/todo-drawer/ModelPicker.tsx` | Tooltip「指定本任务使用的模型」→「指定本事项使用的模型」 |
| `frontend/tests/e2e-test.spec.ts` | 同步两处 placeholder 断言与注释 |
| `frontend/tests/check_template_modal_open.spec.ts` | 复现脚本转正为回归用例：断言抽屉标题为「创建事项」，并守护「从模板创建」弹窗可用性 |

## 验证结果

- `npx tsc --noEmit`：零错误
- `npx vitest run`：全部通过（见 PR 评论留档数字）
- `npx playwright test tests/check_template_modal_open.spec.ts tests/e2e-test.spec.ts`：通过（证据见 PR 评论）

## 已知限制或待改进点

- `QuickCaptureModal` 的「已创建任务」提示为同类残留：rebase 评审时已顺手修复（一处字符串，107 spec 断言前缀「已创建」不受影响）。
- 用户反馈的第二个问题「从模板创建点不开」在两环境三视口均未能复现（弹窗 zIndex 1200 正常浮于抽屉之上、接口 200），已在回归用例中加守护；待用户补充具体环境/表现后另行处理。
