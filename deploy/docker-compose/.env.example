# NTD 服务端口
NTD_PORT=8088

# 数据库路径通过 ~/.ntd/config.yaml (生产) 或 ~/.ntd/config.dev.yaml (开发) 配置
# 不支持通过环境变量直接设置，需修改对应的 config.yaml 文件中的 db_path 字段
# 生产模式默认: ~/.ntd/data.db
# 开发模式默认: ~/.ntd/data.dev.db

# 日志级别：debug, info, warn, error
# RUST_LOG=info